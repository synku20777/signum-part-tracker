[pytest]
addopts = -p no:cacheprovider
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    real_vision_model: downloads and runs the real DINOv2 model when explicitly enabled
